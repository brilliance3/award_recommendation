fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
python-multipart>=0.0.9
jinja2>=3.1
openpyxl>=3.1
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0

# 영업일 계산(발급목표일) — business_days.py
holidays>=0.50

# 도장 PDF 합성 — pdf_seal.py (좌표 추출/그리기/병합)
pdfplumber>=0.11
pypdf>=4.0
reportlab>=4.0

# DB 드라이버 (운영용 Postgres)
psycopg2-binary>=2.9

# PDF 엔진 (배포 환경은 Playwright 기본)
# Playwright는 mcr.microsoft.com/playwright/python 베이스 이미지에 이미 들어있음.
# 로컬에서 별도로 쓸 경우만 명시:
playwright>=1.47

# 로컬에서 WeasyPrint를 쓰고 싶다면 주석 해제 (시스템 라이브러리 필요)
# weasyprint>=60
